import React from "react";
import styled from "styled-components";

const Chart = ({ minPrice, maxPrice }) => {
  return <ChartWrapper></ChartWrapper>;
};

const ChartWrapper = styled.div``;

export default Chart;
